**TDD with angular

How to:

install Karma

`$ npm install karma -g`
`$ karma -–version`
`$ karma init`

$ npm install karma-jasmine -g
$ npm install karma-chrome-launcher -g


$ npm install bower -g
$ bower init

**Installing Angular

$ bower install angular
